name: Focally
options:
  bundleIdPrefix: app.focally
  xcodeVersion: "15.0"
  deploymentTarget:
    macOS: "14.0"
  generateEmptyDirectories: true
  settings:
    base:
      FONT_PROCESSORS: [ttf]
      SWIFT_VERSION: "5.9"
      PRODUCT_BUNDLE_IDENTIFIER: app.focally.mac
      MARKETING_VERSION: "0.9.6"
      CURRENT_PROJECT_VERSION: "79"

targets:
  Focally:
    type: application
    platform: macOS
    sources:
      - Sources/Focally
      - path: Sources/Focally/Resources/AppIcon.icns
        buildPhase: resources
      - path: Sources/Focally/Resources/bell.aiff
        buildPhase: resources
      - path: Sources/Focally/Resources/confirmation_003.aiff
        buildPhase: resources
      - path: Sources/Focally/Resources/focally-focus-off-signed.shortcut
        buildPhase: resources
      - path: Sources/Focally/Resources/focally-focus-on-signed.shortcut
        buildPhase: resources
      - path: Sources/Focally/Resources/glass_005.aiff
        buildPhase: resources
      - path: Sources/Focally/Resources/pluck_002.aiff
        buildPhase: resources
      - path: Sources/Focally/Resources/Fonts/Inter-Bold.ttf
        buildPhase: resources
      - path: Sources/Focally/Resources/Fonts/Inter-Medium.ttf
        buildPhase: resources
      - path: Sources/Focally/Resources/Fonts/Inter-Regular.ttf
        buildPhase: resources
      - path: Sources/Focally/Resources/Fonts/Inter-SemiBold.ttf
        buildPhase: resources
      - path: Sources/Focally/Resources/en.lproj
        buildPhase: resources
      - path: Sources/Focally/Resources/es.lproj
        buildPhase: resources
      - path: Sources/Focally/Resources/pt.lproj
        buildPhase: resources
    settings:
      base:
        INFOPLIST_FILE: Sources/Focally/Info.plist
        CODE_SIGN_ENTITLEMENTS: Sources/Focally/Focally.entitlements
        PRODUCT_BUNDLE_NAME: Focally
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_COLOR_SET_NAME: focally
        COMBINE_HIDPI_IMAGES: true
        ENABLE_PREVIEWS: true
        SWIFT_EMIT_LOC_STRINGS: true
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: false
        MARKETING_VERSION: "0.9.6"
        CURRENT_PROJECT_VERSION: "79"
    entitlements:
      path: Focally/Focally.entitlements
      properties:
        com.apple.security.automation.apple-events: true
    dependencies: []
  FocallyTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/FocallyTests
    resources:
      - path: Sources/Focally/Resources/bell.aiff
      - path: Sources/Focally/Resources/confirmation_003.aiff
      - path: Sources/Focally/Resources/glass_005.aiff
      - path: Sources/Focally/Resources/pluck_002.aiff
    dependencies:
      - target: Focally
      - sdk: XCTest.framework
      - sdk: Cocoa.framework
    settings:
      base:
        INFOPLIST_FILE: Tests/FocallyTests/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: app.focally.mac.Tests
        PRODUCT_BUNDLE_NAME: FocallyTests
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: false
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        SWIFT_VERSION: "5.9"
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks @loader_path/../Frameworks"
  FocallyUITests:
    type: bundle
    platform: macOS
    productType: com.apple.product-type.bundle.ui-test
    sources:
      - Tests/FocallyUITests
    dependencies:
      - target: Focally
      - sdk: XCTest.framework
    settings:
      base:
        INFOPLIST_FILE: Tests/FocallyUITests/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: app.focally.mac.UITests
        PRODUCT_BUNDLE_NAME: FocallyUITests
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: false

schemes:
  Focally:
    build:
      targets:
        Focally: all
        FocallyTests: [test, analyze]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - FocallyTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
